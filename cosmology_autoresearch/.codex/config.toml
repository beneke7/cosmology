# Checked against official configuration documentation on 2026-09-23.
# Preserve the user's existing permissions, authentication and sandbox policy.
model = "gpt-6-astra"
model_reasoning_effort = "max"
web_search = "live"

[agents]
enabled = true
max_concurrent_threads_per_session = 6
default_subagent_model = "gpt-6-luna"
default_subagent_reasoning_effort = "max"
